<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

        <import type="android.graphics.Typeface" />

        <import type="com.internship.firstweekapp.ui.calendar_dialog.Type" />

        <import type="com.internship.firstweekapp.ui.calendar_dialog.State" />

        <variable
            name="model"
            type="com.internship.firstweekapp.ui.calendar_dialog.MiniModel" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="10dp"
        android:background="@{model.state == State.SELECTED ? @color/app_color : @color/white}">

        <com.google.android.material.textview.MaterialTextView
            android:id="@+id/text"
            android:layout_width="27dp"
            android:layout_height="23dp"
            android:text="@{model.text}"
            android:textAlignment="center"
            setType="@{model.type}"
            setState="@{model.state}"
            android:textSize="17sp"
            setStyle="@{model.type == Type.NameOfWeek ? Typeface.BOLD : Typeface.NORMAL}"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>